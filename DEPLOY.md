# 部署成网站（Cloud Studio）

## 先看这一条，它比部署步骤重要

**学习进度不会跟着网站走。**

工作台的进度存在浏览器的 localStorage 里，而 localStorage 是**按网址隔离**的：

| 你从哪里打开 | 进度存在哪里 |
|---|---|
| 双击 `index.html`（`file://`） | 本地文件来源 |
| `http://localhost:3000` | 本地服务器来源 |
| Cloud Studio 给的公网网址 | 那个域名的来源 |

这三者是三个互不相通的抽屉。你在本地练了十道题，打开网站是看不到的。

**所以：换地方之前，先在「数据与备份」里导出 JSON；在新地址里导入。**这是唯一的搬运方式，也是我为什么不把「云端同步」做进去的原因——那需要一个后端和账号系统，而现在这个工作台是纯本地、零服务器的。

## 我已经准备好的部分

| 文件 | 作用 |
|---|---|
| `serve.js` | 零依赖静态服务器。本地和云端都是同一条命令 |
| `package.json` | 没有任何依赖，只为了让平台能识别这是一个 Node 项目 |
| `.gitignore` | 排除了浏览器临时目录 |
| `.git` | 已经初始化并提交（21 个文件，1 个提交） |

`serve.js` 做了三件云端部署需要的事：监听 `0.0.0.0`（只监听 127.0.0.1 的话端口转发拿不到流量）、正确设置 MIME 类型（否则 `.js` 可能被当成纯文本、页面白屏）、拒绝目录穿越和不对外提供的目录。

本地先验一遍：

```powershell
cd C:\Users\陈鑫\Desktop\skill
node serve.js
```

浏览器打开 `http://localhost:3000`，确认和双击打开的效果一样。已经验证过：首页、脚本、课程数据都是 200，越权和穿越请求一律 404。

## 部署到 Cloud Studio

Cloud Studio 是云端 IDE，它运行在一台远程 Linux 容器里，**看不到你电脑上的文件**。所以第一步必须先把文件送上去，有两条路。

### 路线 A：从 Git 仓库导入（推荐）

1. 把本地仓库推到 GitHub 或 Gitee（新建一个空仓库，**不要**勾选初始化 README）：

   ```powershell
   cd C:\Users\陈鑫\Desktop\skill
   git remote add origin https://github.com/你的用户名/mew-workbench.git
   git branch -M main
   git push -u origin main
   ```

   这一步需要你的 GitHub 账号和授权，我这边做不了。

2. 打开 [cloudstudio.net](https://cloudstudio.net)，用微信或腾讯云账号登录。

3. 新建工作空间 → 选择「从 Git 仓库导入」（或叫「导入仓库」），填上刚才的仓库地址。

4. 等容器启动完成，进入一个 VS Code 风格的界面。

5. 在底部终端里运行：

   ```bash
   node serve.js 3000
   ```

   看到 `机械工程师工作台已启动：http://localhost:3000` 就是成功了，**这个终端不要关**。

6. 找到「端口」面板（通常在右侧或底部标签栏里，也可能叫「预览」）。Cloud Studio 一般会自动检测到 3000 端口并给一个可点击的地址。如果没有自动出现，手动添加端口 `3000`。

7. 点那个地址，你会拿到一个形如 `https://xxxx.cloudstudio.xxx` 的公网链接。**这个就是你的网站。**

### 路线 B：直接在 Cloud Studio 里上传文件

不想碰 Git 的话：新建一个空工作空间（选 Node.js 模板），在左侧文件树里把 `index.html`、`app.js`、`curriculum.js`、`serve.js`、`package.json` 这五个文件拖上去，然后跳到上面的第 5 步。

`mcp-glm` 目录**不要传**——那是需要 API Key 的服务端程序，跟网站无关，`serve.js` 也已经在对外服务里排除了它。

## 界面文字对不上怎么办

Cloud Studio 的界面会改版，我写的是概念：**找「从 Git 导入」、找终端、找「端口」。**这三个东西在任何一个云端 IDE 里都存在，只是叫法不同。

## 上线之后立刻做两件事

1. 在网站上打开「数据与备份」，点一次「导出进度（JSON）」。以后每换一次打开方式（本地 ↔ 网站），都先导出再导入。
2. 把公网地址存成书签。手机也能打开，密排表格会横向滚动，不影响使用。

## 一句实话

Cloud Studio 适合边改边看（它本质是个云端 IDE），但**免费版的工作空间会休眠、也可能被回收**，链接不保证长期有效。如果你要的是一个能一直打开的网址，`GitHub Pages`、`Cloudflare Pages` 或 `Vercel` 更合适——这个项目是纯静态的，不需要 Node 服务器也能跑（`serve.js` 只是本地预览用的），把仓库连上去就能自动发布，而且能绑自己的域名。

想让我改走那条路的话说一声，配置我可以直接写好。
